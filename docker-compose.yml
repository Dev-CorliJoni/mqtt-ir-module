services:
  mqtt-ir-module:
    build: .
    container_name: mqtt-ir-module
    restart: unless-stopped

    devices:
      - "/dev/lirc0:/dev/lirc0"

    volumes:
      - "./data:/data"

    environment:
      - LEARN_TIMEOUT_S_DEFAULT=600
      # Optional:
      # - API_KEY=change-me

    ports:
      - "8000:8000"